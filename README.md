# NWC RAG Website

الواجهة الأمامية للمساعد الذكي لشركة المياه الوطنية (NWC) — مبنية بـ JavaScript و HTML و CSS، تتصل بواجهة RAG الخلفية على المنفذ `8000`.

## 1. تشغيل واجهة RAG

من مجلد المشروع الرئيسي:

```powershell
cd C:\Users\essa4\Desktop\projects\pdf
.\.venv\Scripts\python.exe embedding\api.py
```

يرد على:

- `GET /health` — للتحقق من الحالة.
- `POST /ask` — لطرح سؤال: `{"question": "..."}` ويرجع `{"answer": "...", "sources": [{"text": "...", "score": 0.0}]}`.

> أول سؤال قد يستغرق وقتًا أطول لأن النموذج يُحمَّل عند أول طلب. الأسئلة اللاحقة تعيد استخدامه.

## 2. تشغيل الموقع

افتح نافذة PowerShell أخرى:

```powershell
cd C:\Users\essa4\Desktop\projects\pdf
python -m http.server 5500
```

ثم افتح في المتصفح:

```text
http://127.0.0.1:5500/rag-website/
```

## 3. الشعار

ضع شعار الشركة في:

```text
assets/nwc logo.jpg
```

(المسار النسبي من مجلد `rag-website/` هو `../assets/nwc logo.jpg`). إذا لم يكن الملف موجودًا، يعرض الموقع علامة النص `NWC` كبديل.

## المزايا

- محادثة بواجهة رسائل (فقاعات) بجانب المستخدم والمساعد.
- مؤشر كتابة متحرك أثناء انتظار الرد.
- أسئلة مقترحة قابلة للنقر.
- عرض المصادر المستخدمة في كل رد (قابلة للطي).
- تبديل الوضع الليلي/النهاري مع الحفظ في المتصفح.
- حالة اتصال API في الشريط العلوي.
- تحقق من الحالة تلقائيًا عند فتح الصفحة.
